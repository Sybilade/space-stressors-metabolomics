version: '3.8'

services:
  # Example service running your Python container
  app:
    build: .
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    environment:
      - ENV=development

  # Example database service
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: mydatabase
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata:
